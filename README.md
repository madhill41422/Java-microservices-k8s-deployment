# Java-microservices-k8s-deployment
DevOps hands-on project demonstrating a real deployment workflow: Java microservices → Maven build → Docker image → Docker Hub → Kubernetes deployment using production-style Deployments/Services, with reusable Kubernetes YAML manifests for scalable service exposure on Minikube (EC2).
